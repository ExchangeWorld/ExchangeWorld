'use strict';

const followModule = require('./follow.module');
followModule.controller('FollowController', FollowController);

/** @ngInject */
function FollowController(follow, $state) {
	var vm         = this;

	/////////////

}
